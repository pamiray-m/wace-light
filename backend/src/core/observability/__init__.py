"""W5.1 — Cross-cutting observability primitives.

`prom.py` exposes lightweight labeled counters and gauges with a Prometheus
text-format renderer. See `prom.py` and the `/metrics` route for usage.
"""
